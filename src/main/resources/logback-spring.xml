<?xml version="1.0" encoding="UTF-8" ?>
<configuration>
    <property name="logPath" value="logs/"/>
    <property name="fileName"		value="test-log" />
    <property name="maxHistory"		value="30" />
    <property name="maxFileSize"	value="5KB" />
    <property name="totalSizeCap"	value="1GB" />
    <property name="consolePattern"	value="[%-5level] [%d{yyyy-MM-dd HH:mm:ss}] [%thread] %logger[%method:%line] - %msg%n"/>
    <property name="filePattern"    value="[%d{yyyy-MM-dd HH:mm:ss}] [%-3relative] [%thread] %-5level %logger{35} - %msg%n"/>

    <springProfile name="console-logging">
        <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
        <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>${consolePattern}</pattern>
            </encoder>
        </appender>
    </springProfile>

    <springProfile name="file-logging">
        <appender name="file" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${logPath}/${fileName}.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${logPath}/%d{yyyy-MM-dd}/%d{yyyy-MM-dd}_${fileName}.%i.log</fileNamePattern>
                <maxHistory>${maxHistory}</maxHistory>
                <maxFileSize>${maxFileSize}</maxFileSize>
                <totalSizeCap>${totalSizeCap}</totalSizeCap>
            </rollingPolicy>
            <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
                <pattern>${filePattern}</pattern>
            </encoder>
        </appender>
    </springProfile>

    <!-- Log Level Module -->
    <springProfile name="high-level-logging">
        <logger name="root" level="error" />
        <logger name="org.springframework" level="error" />
        <logger name="me.moon.boilerplate" level="warn" />
    </springProfile>

    <springProfile name="low-level-logging">
        <logger name="root" level="info" />
        <logger name="org.springframework" level="info" />
        <logger name="me.moon.boilerplate" level="debug" />
    </springProfile>

    <root>
        <springProfile name="console-logging">
            <appender-ref ref="console" />
        </springProfile>

        <springProfile name="file-logging">
            <appender-ref ref="file" />
        </springProfile>
    </root>
</configuration>